import { MyyFilterPipe } from './myy-filter.pipe';

describe('MyyFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MyyFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
